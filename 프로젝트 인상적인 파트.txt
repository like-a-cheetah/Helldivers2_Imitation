블로그 만들기

ai predict

렌더 타겟 만든 방법 적기(즐찾에 있음)

OnClicked, OnPressed()의 차이점 (IsPressed())

스트라타젬 버튼들 찾는 방식(WIdgetTree에 대해서 찾고 쓰기)

새로운 맵을 로드하면, 기존의 객체들은 전부 사라짐, GameInstance를 이용해 이를 관리,

DuplicateObject로 깊은 복사

위젯이 화면에 나타나려고 하는 순간에 NativeConstruct 함수가 호출, CreateWidget으로 User위젯을 만들어도 Box->AddChild(StratagemCondition); 이런 상황에 NativeConstruct 함수가 호출됨
위젯에서 UUserWidget이 아닌 그냥 버튼같은거 만들 때는 WidgetTree::ConstructWidget 사용

Constraint 작업

BTService FBlackboardKeySelector 오류

BTT_MoveTo_Sequence 클래스
태스크 노드 bCreateNodeInstance = true로 설정 
UnregisterObserver

Turret_MeshComp1->RefreshBoneTransforms();로 위치 바로 계산

FireSubsystem

총알 충돌 구현방식

로켓 NewObjet<스피어컴포넌트> 
SplashCollision->RegisterComponent();
SplashCollision->UpdateOverlaps();

MacroWidgetComponent 설계 쓰기
ClassGroup=(Custom), meta=(BlueprintSpawnableComponent)이거 왜 써야하는지 적기
Interact_Implementation 원리 작성
if (IObjectInterface* Object = Cast<IObjectInterface>(Obj))
		{
			IObjectInterface::Execute_Interact(Obj, this);

	UFUNCTION(BlueprintCallable)
	void Interact(TScriptInterface<class IPlayerControl> Player);
	void Interact(class IPlayerControl* Player)
이거 인터페이스 
UINTERFACE(BlueprintType)
해줘야하는거

AddWeakLambda(this, [](){})